## Configura\u00e7\u00f5es do spring para conex\u00e3o com o banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/challenge?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username =root
spring.datasource.password =2418151993

# Hibernate

# Faz com que o Hibernate gere um SQL melhor para o banco de dados escolhido
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE